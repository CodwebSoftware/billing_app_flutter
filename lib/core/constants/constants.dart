import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

const String softwareId = "Billing_v_1.0.0";
const String softwareAmount = "2999";

const String BASE_URL = "http://localhost:3000/api";

// Colors used in this app
const primaryColor = Color.fromRGBO(17, 159, 250, 1);
const secondaryColor = Colors.white;
const snackbarBackgroundColor = Colors.black54;
const bgColor = Color.fromRGBO(240, 255, 230, 1.0);
const textColor = Colors.black;
const lightTextColor = Colors.black26;
const transparent = Colors.transparent;
const snackbarTextColor = Colors.white;

const iconColor = Color.fromRGBO(99, 174, 234, 1.0);

const dividerBeltColor = Color.fromRGBO(99, 174, 234, 1.0);

const editableTextColor = Color.fromRGBO(99, 174, 234, 0.5);

const grey = Color.fromRGBO(179, 183, 186, 1.0);
const purple = Color.fromRGBO(165, 80, 179, 1);
const orange = Color.fromRGBO(251, 137, 13, 1);
const green = Color.fromRGBO(51, 173, 127, 1);
const red = Colors.red;
final lightGreen = Colors.green[100];
final lightYellow = Colors.yellow[100];
final lightRed = Colors.red[100];

// Default App Padding
const appPadding = 16.0;

// Date Format
var df = DateFormat("dd-MM-yyyy");
var dftm = DateFormat("dd-MM-yyyy H:mm a");
